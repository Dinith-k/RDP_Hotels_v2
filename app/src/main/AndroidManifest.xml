<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dinith.rdp_hotels">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RDP_Hotels">
        <activity android:name=".ui.checkout.checkout"></activity>
        <activity android:name=".ui.menu.view_food" />
        <activity android:name=".ui.dashboard.room_customize" />
        <activity android:name=".ui.dashboard.select_room" />
        <activity android:name=".ui.menu.check_out" />
        <activity android:name=".ui.payments" />
        <activity android:name=".ui.menu.menu" />
        <activity android:name=".login.register" />
        <activity android:name=".login.login" />
        <activity android:name=".login.welcome" />
        <activity android:name=".login.splash_s">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
    </application>

</manifest>